# AGIFORMER Text-Only Model Configuration
# Text processing with memory backbone, no multimodal or introspection

defaults:
  - base

# Feature flags for text-only variant
use_memory: true
use_introspection: false
use_multimodal: false
use_linear_attention: false
