# AGIFORMER Full Model Configuration
# All features enabled: memory, introspection, multimodal, linear attention

defaults:
  - base

# Feature flags for full variant (all features enabled)
use_memory: true
use_introspection: true
use_multimodal: true
use_linear_attention: true
